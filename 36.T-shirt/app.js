function make_shirt(size, message) {
    console.log("Size:" + size, "MESSAGE:" + message);
}
make_shirt("medium\n", "\nONE DAY\nYOU'LL\nKNOW MY NAME."); // Calling the function with arguments "XL" for size and "Hello World" for message
